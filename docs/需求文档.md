随机相册回忆APP（Flutter）需求文档
1. 项目概述
1.1 项目名称
RePhoto | 重逢

1.2 项目目标

开发一款跨平台（Android + iOS）的 Flutter 应用，从用户本地相册中每日随机抽取若干照片进行展示，并允许用户对照片进行回忆记录管理。

核心目标：

提升用户“照片回忆触发体验”
将“随机浏览照片”变为“带时间线的记忆回顾”
2. 技术栈要求
前端框架：Flutter（必须）
语言：Dart
平台：Android / iOS（单代码库）
本地存储：
sqflite
权限管理：
相册权限（Android + iOS）
3. 核心功能模块
从用户相册中随机抽取照片，用户可以对照片进行浏览、删除或者填写记录。如果下一次抽到之前填写过记录的照片则可以展开查看之前的记录。

# 照片记录功能设计
去掉之前预留的hive改用sqflite数据库。
创建两张表，一张用来存放照片浏览信息、另一种用来存放照片记录
```浏览信息
photo_views{
    photoId    照片id
    viewCount    浏览次数
    firstViewedAt   第一次浏览时间
    lastViewedAt    最后一次浏览时间
}
```
```照片记录
CREATE TABLE records (
id        主键
photoId    照片id
content     记录内容
mood        情绪  
createdAt   创建时间
updatedAt   更新时间
)
```

😊 开怀 / 😌 平静 / 🥲 怀念 / 😢 感伤 / 🤩 惊喜 / 😅 尴尬

# 记录提示语 （待定）
给记录提供思路灵感，降低开始成本
"这张照片让我想起……"
"如果能回到那个瞬间，我想说……"
"照片里的人现在……"
"那天的天气是……"
"和谁在一起……"

# 时光胶囊（待定）
记录界面可以设置是否开启时空，功能如下
开启时空胶囊后可以选择两种模式，一种是指定具体时间，另一种是多久时间以后，到了指定的时候会触发强制抽取时空胶囊记录的照片。

# 重逢功能（用户选择开启） （待定）
再次抽到同一张照片时触发提示
首次抽到 → 正常展示
第二次抽到 → 弹出提示："这张照片你 47 天前看过，当时你写下了这段回忆"
第三次抽到 → "这是你第三次与这张照片重逢"


# 回顾统计（待定）
今日已看：12 张
累计看过：847 张
写过记录：23 张


# 历史上的今天（用户选择开启） （待定）
如果一张照片的拍摄日期恰好是今天的日期（不同年份），优先展示并加上特殊标记

# 模糊回忆（用户选择开启）（待定）
抽取到有记录的照片时照片加上高斯模糊，擦拭照片后才显示清晰的图片

# 照片年龄（用户选择开启）（待定）
提示 拍摄于 2023-03-15，今天是 2025-07-20 → "2 年 4 个月前"

# 时间对撞（用户选择开启）（待定）
开启功能后概率出现抽取一张照片后再抽取一张几年前相同日期的照片并排显示